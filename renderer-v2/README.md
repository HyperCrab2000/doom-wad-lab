# Renderer-V2

GZDoom-derived renderer core. Native OpenGL first, then WASM + WebGL2.

This directory is **new work only** — do not modify existing `src/wad/renderer/` until opt-in integration is explicitly approved.

See `docs/gzrender-v2/architecture-rules.md`.
